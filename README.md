# Decision workshops

One page for [workshops.andypbrowne.com](https://workshops.andypbrowne.com/). Eleventy, hosted on Netlify.

## Quick start

1. `npm install`
1. `npm start` — http://localhost:8080/
1. `npm run build` — writes to `_site`
